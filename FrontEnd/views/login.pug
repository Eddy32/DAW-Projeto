extends layout

block content
  h3 Agenda de Eventos: Login
  form(action="/login" method="POST")
    input(type="text" name="email")
    input(type="password" name="password")
    input(type="submit" value="Enviar")
  a(href='http://localhost:5004/register') Registar
    
